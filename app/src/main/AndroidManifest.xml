<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mobile_prog.team_planner">
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <application android:theme="@style/Theme.ProjectMoprog"
        android:usesCleartextTraffic="true"
        android:name="com.mobile_prog.AdsHuawei"
        >
        <activity
            android:name="com.mobile_prog.team_planner.RegisterActivity"
            android:exported="false" />
        <activity
            android:name="com.mobile_prog.team_planner.SplashScreen"
            android:exported="true"
            android:label="TODOIST " />
        <activity
            android:name="com.mobile_prog.team_planner.LoginActivity"
            android:allowBackup="true"
            android:exported="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/Theme.ProjectMoprog.SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.mobile_prog.team_planner.MainActivity"
            android:exported="false" />
    </application>

</manifest>